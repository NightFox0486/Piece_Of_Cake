import 'package:flutter/material.dart';

class BuyCreate extends StatelessWidget {
  const BuyCreate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('BuyCreate')
      ),
    );
  }
}