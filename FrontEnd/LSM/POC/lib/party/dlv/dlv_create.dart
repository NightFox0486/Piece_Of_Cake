import 'package:flutter/material.dart';

class DlvCreate extends StatelessWidget {
  const DlvCreate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text('DlvCreate')
      ),
    );
  }
}